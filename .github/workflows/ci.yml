name: CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  build:
    env:
      NIMBLE_COMMIT: v0.20.1
    uses: status-im/nimbus-common-workflow/.github/workflows/common.yml@main
