name: CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: status-im/nimbus-common-workflow/.github/workflows/common.yml@main
    with:
      nimble-version: 0fea1a5e112b87d6ebec5a01327a003388d2e425
      test-command: |
        nimble setup -l
        nimble test
