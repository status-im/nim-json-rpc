name: CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: status-im/nimbus-common-workflow/.github/workflows/common.yml@verbose-build
    with:
      nimble-version: 95f2b9df8ffbdaa98fdbd87e4498253ac8c29ca4
      test-command: |
        nimble setup -l
        nimble test
